{
  "name": "johnpgame",
  "private": true,
  "workspaces": [
    "src/client",
    "src/server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=src/client\" \"npm run dev --workspace=src/server\"",
    "build": "npm run build --workspace=src/client && npm run build --workspace=src/server",
    "test": "npm run test --workspace=src/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
